node_modules
/renderer/out
/renderer/.next
/app/dist
/dist
/dist-js
vendor/*
# gifski is downloaded on postinstall; the mac-* helper binaries are custom
# universal rebuilds that must be committed (see scripts/build-mac-helpers.sh)
!vendor/mac-helpers
